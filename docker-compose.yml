version: '3.8'

services:
  db:
    image: postgres:15
    container_name: hw_monitor_db
    restart: always
    environment:
      POSTGRES_USER: pedroadmin
      POSTGRES_PASSWORD: senha_segura123
      POSTGRES_DB: hardware_monitor
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: